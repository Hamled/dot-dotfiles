#!/usr/bin/env bash

function run() {
    local APP_NAME="$(basename "$0")"
    local APP="$(which -a "$APP_NAME" | sed "0,\|^$0\$|d" | head -n 1)"
    [[ ! -e "$APP" ]] && echo "$APP_NAME could not be found" && exit 1

    exec $APP --force-device-scale-factor=2 ${@}
}

run
